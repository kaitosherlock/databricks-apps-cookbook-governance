[pytest]
# The app is a single source tree under governance-app/; tests live outside it
# so the deployed bundle stays free of test fixtures.
pythonpath = governance-app tests
testpaths = tests
addopts = -q
filterwarnings =
    ignore::DeprecationWarning
